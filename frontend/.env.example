# Leave unset: the UI calls /api/* on its own origin (FastAPI serves the
# bundle in production, Vite proxies to uvicorn:8000 in dev).
# Set this only to point the UI at a backend on a different host.
# VITE_API_BASE=https://nebula-x-xxxxxxxx.a.run.app
